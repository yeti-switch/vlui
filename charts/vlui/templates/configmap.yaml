{{- if not .Values.existingConfigMap }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vlui.fullname" . }}-config
  labels:
    {{- include "vlui.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- include "vlui.renderedConfig" . | nindent 4 }}
{{- end }}
